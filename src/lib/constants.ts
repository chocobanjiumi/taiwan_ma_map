// Taipei city center coordinates
export const TAIPEI_CENTER: [number, number] = [25.033, 121.5654];

// Default map zoom level
export const DEFAULT_ZOOM = 13;

// Search radius in meters (default 10km covers Taipei + some New Taipei)
export const DEFAULT_RADIUS = 10000;

// Price range bounds (NTD)
export const PRICE_MIN = 0;
export const PRICE_MAX = 200000;
export const PRICE_STEP = 1000;

// Map tile URL (OpenStreetMap)
export const TILE_URL = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png';
export const TILE_ATTRIBUTION =
  '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors';
